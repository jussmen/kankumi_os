# Domain Context: Kankumi OS

**内部名**: Kankumi OS（カンクミOS）
**サービス名**: 未定（リリース前に確定予定）
**リポジトリ**: kanri_saas

## 用語集（Glossary）

### Organization（管理組合）
マンションの区分所有者全員で構成される任意団体。本SaaSにおけるテナント単位。1マンション = 1 Organization。

### FreeTrial（無料トライアル）
新規 Organization が有料契約前に体験できる制限付きプラン。Unit 数上限5室。住民体験（ResidentProfile登録・共有画面閲覧）・会計支援体験（CSV インポート・入金 Dashboard・支出管理）・業務支援体験（OperationsCalendar・AnnualChecklist）の3領域を体験できる。CSV インポートは5行以内での体験を想定。発見経路は理事長または理事の一人が認知し、自身でトライアルを開始するケースを主な想定とする。

### PrivacyCompliance（個人情報保護法対応）
SaaS運営者は管理組合から個人情報の取扱を委託される立場（取扱受託者）。住民への説明責任は管理組合側にある。必要書類：プライバシーポリシー・利用規約・個人情報取扱委託契約雛形・住民の初回ログイン時同意フロー。リリース80%時点で弁護士監修を依頼予定。それまではPlaceholderコンテンツを表示する。要配慮個人情報（緊急情報セクション）は取得時に別途同意チェックを設ける。

### Subscription（サブスクリプション）
Organization が SaaS に支払う利用契約。200円/戸/月・最低4,000円/月、年払い・銀行振込（請求書払い）。申し込みはフォームまたはメール経由で受け付け、SaaS 運営者に通知メールが届き、手動で請求書を発行・入金確認する。Stripe への移行は将来的に検討する。年払いにより総会での予算承認サイクルと合致する。

### DataExport（データエクスポート）
管理者（admin・vice_president）はいつでも住民台帳・収支データ・入金記録をCSV形式でエクスポートできる。解約時のデータ持ち出しを保証することで導入時の心理的障壁を下げる。積み上がった議事録・意思決定ログは構造化データとして他システムに移行しにくいため、エクスポート機能を持ちつつも実質的なロックインは維持される。

### Onboarding（オンボーディング）
新規 Organization がシステムを使える状態にするまでのセットアップフロー。ウィザード形式で順を追って実施する。Unit台帳・UnitCharge はExcelからのCSVインポートで一括登録できる。完了率がSaaSの初期解約率に直結するため、完了状態の可視化（進捗インジケーター）を設ける。

### Unit（区画・部屋）
マンション内の各専有部分。部屋番号・専有面積・OccupancyStatus を持つ。

### OccupancyStatus（廃止）
~~Unit の入居・管理状態。~~ 廃止。台帳に登録された Unit はすべて管理費等の支払い対象とみなす。管理費免除の Unit は台帳に登録しない。入金照合対象かどうかは DataReadiness で判定する。

### UnitOwner（区分所有者）
Unit の法的所有者。居住しているとは限らない。SaaSアカウントを持たない場合もある（user_id は nullable）。owner_type により「所有者のみ」「居住者のみ」「所有かつ居住」を区別する。

### ResidentOnboarding（住民オンボーディング）
新規住民がシステムを使える状態になるまでのフロー。以下の3パターンがある：
- **役員&&住民**（基本）：管理者が役員ロールかつ部屋番号を指定して招待 → 初回ログインで `/my/setup` → 役員業務と台帳が同時に有効化
- **役員のみ先行**（1-b）：管理者が役員ロール・部屋なしで招待 → 役員業務開始 → 後で部屋を紐づけると `/my/setup` が要求される
- **住民→役員昇格**（1-c）：住民として招待・setup 完了 → 管理者がロールを役員に変更

`/my/setup` へのリダイレクト条件：ロールに関わらず「unit_owner レコードがあって name_kana が未入力」であれば setup を要求する。

### MoveOut（転出）
住民が退去するフロー。部屋詳細画面から「住民を解除する」操作で実行。`unit_owners.end_date` をセット・`organization_members.is_active = false`。転出後、該当 Unit の台帳ステータスは「要確認」に戻る。新住民の招待・setup 完了で「台帳有効」に復帰する。

### MoveIn（転入）
退去後の Unit に新規住民を招待するフロー。部屋詳細画面から招待メールを送信。新住民が setup を完了すると台帳ステータスが「台帳有効」になる。

---

## ロールと権限

### BoardMember（理事）
管理組合の理事会を構成するメンバー。OrganizationMember の role が `admin`・`vice_president`・`treasurer`・`board_member` のいずれか。

### ロール定義

| role | 名称 | 概要 |
|------|------|------|
| `admin` | 理事長 | 全権限。SaaS契約・課金管理・メンバー削除・根幹設定の変更が可能な唯一のロール |
| `vice_president` | 副理事長 | admin とほぼ同等だが、アカウント削除・SaaS課金設定・組合の根幹設定変更は不可 |
| `treasurer` | 会計担当理事 | 入金管理・収支管理・CSVインポートが可能。組合設定・メンバー管理は不可 |
| `board_member` | 理事 | 入金ダッシュボード閲覧（編集不可）・Topic作成・Announcement配信が可能 |
| `auditor` | 監事 | 全財務データへの読み取り専用アクセス。AuditReport のアップロードが可能。編集権限なし |
| `resident` | 住民 | 自室の ResidentProfile 編集・all_members の Topic閲覧・コメントが可能 |

### Treasurer（会計担当理事）
収支管理・入金確認を主に担当する BoardMember。role = `treasurer`。

### Resident（住民）
Unit に居住する人。区分所有者（UnitOwner）と同一の場合もある。role = `resident`。基本的に自身でアカウントを登録し、ResidentProfile を更新する。管理者による代行登録も可能。

---

## 住民情報

### ResidentProfile（住民プロフィール）
住民が自身で登録・更新する情報の集合体。PaymentProfile（支払い情報）と HouseholdProfile（世帯情報）で構成される。管理者は全 ResidentProfile を参照可能。住民は自室の情報のみ参照・編集可能。登録・更新は `/my/profile` で行う（初回は必須、2回目以降は任意）。ロールに関わらず unit_owner レコードがあって name_kana が未入力の場合はアクセス時に `/my/profile` へリダイレクトされる。

### HouseholdProfile（世帯プロフィール）
住民が登録する世帯情報。目的別セクションで構成され、各セクションに取得目的が明記される。これにより住民の入力動機と個人情報保護法上の取得目的明示を同時に満たす。

セクション構成：
- **基本情報（必須）**: 住民代表者氏名・連絡先
- **緊急時対応情報（任意）**: 災害・緊急時に管理組合が必要とする情報
- **支払い関連情報**: 駐車場利用有無・ナンバープレート、バイク置き場利用有無・ナンバープレートなど施設利用状況
- **その他（カスタムフィールド）**: 管理組合ごとに定義可能な追加項目（ペットの有無など）

### CustomProfileField（カスタムプロフィール項目）
管理組合が Organization ごとに定義する追加の住民情報項目。項目名・種別（テキスト/選択肢/真偽値）・目的説明・任意/必須を設定できる。

---

## 入金管理

### ManagementFee（管理費）
毎月各 Unit から徴収する費用。建物の日常的な管理・運営に充てる。

### ReserveFund（修繕積立金）
毎月各 Unit から徴収する費用。将来の大規模修繕に備えた積立。ManagementFee とは会計上別管理。

### ChargeType（請求項目種別）
Organization レベルで定義する固定費の種別。`management_fee`（管理費）・`reserve_fund`（修繕積立金）・`common_fee`（共用費）・`parking`（駐車場）・`bike_parking`（バイク置き場）・`other`（その他）の固定値。`other` には管理者がエイリアス名を設定できる。

### UnitCharge（部屋別請求設定）
Unit ごとに適用される ChargeType と金額の組み合わせ。有効期間（effective_from / effective_to）を持ち、年度内の金額変動に対応する。特定月の ExpectedAmount は、その月に有効な UnitCharge の合計として計算される。

### ExpectedAmount（期待入金額）
特定月に有効な UnitCharge の合計として動的に計算される月次期待振込金額。固定値ではなくクエリ結果として導出される。DataReadiness の判定に使用される。

### IrregularFee（イレギュラー施設使用料）
共用施設の一時利用料など、毎月の定期振込とは別に発生する費用。月次入金管理の対象外。Expense として別途記録する。

### PaymentProfile（支払者プロフィール）
Unit に紐づく振込情報。振込名義（カタカナ）・銀行名を保持する。BankTransaction との自動マッチングに使用される。source が `admin` のプロフィールが `user` より優先される。口座変更時に過去データとの整合性を保つため、有効期間（effective_from / effective_to）を持つ。1つの Unit に複数登録可能（家族名義・法人名義・変更履歴など）。

**振込名義の照合正規化**: `transfer_name` の保存値はオリジナル表記のまま維持する。マッチング時のみ両辺（`transfer_name` と BankTransaction の摘要）を半角カナに正規化して比較する。アルファベット表記（銀行の文字数制限で切れる場合あり）は前方一致で照合する。

**口座振替（集金サービス）との関係**: SMBC等の振替サービスを利用している組合では、銀行明細に個人名が出ないため上記の名義照合が使えない。振替サービス業者が提供する集金明細CSVを別途インポートする方式を検討中。SMBCのデータフォーマット確認待ちで実装保留（2026-05-26時点）。

### DataReadiness（データ準備状態）
入金マッチングを実行するために Unit ごとに必要な事前設定の充足状態。以下の2要素が揃った Unit を「台帳有効」とし、セミオート入金確認の対象とする：
1. **月額料金設定済み**（UnitCharge が全項目設定されている）
2. **初回住民確認済み**（`unit_owners.charge_confirmed_at IS NOT NULL` — 住民が初回 setup 時に一度だけ確認）

月額料金の変更は管理者が単独で完結させる（住民の再確認は不要。`charge_confirmed_at` はリセットしない）。台帳ステータスの3値：台帳有効（全条件充足）・要確認（料金設定済みだが初回確認未完了）・未設定（料金未設定）。

### ProfileHistory（プロフィール履歴）
管理者が参照できる Unit ごとの振込情報変更履歴。PaymentProfile は有効期間（effective_from/effective_to）を持つため DB には全履歴が保存されている。管理者向け部屋詳細画面に過去の振込情報を折りたたみ表示する。氏名・ロール変更の履歴は MVP 対象外。

### BankImport（銀行CSVインポート）
管理組合の銀行口座の取引明細CSVをアップロードする操作。1回のインポートが1 BankImport。インポート時に対象口座（BankCSVMapper）を選択する。1組合が複数口座（管理費・修繕積立金など）を持つケースに対応。

### BankCSVMapper（銀行CSVマッパー）
銀行ごとに異なるCSVフォーマットを吸収する設定。**プリセット方式**を採用：`preset_key`（`resona` / `mizuho` / `yokohama-bank` など）と文字コードのみをDBに保持し、各銀行固有のパース処理（日付フォーマット・入金フィルター・列構成）はコードで管理する（ADR-0005）。未対応銀行は管理者が列設定を手動入力するが、日付3列分割・可変ヘッダなどの特殊フォーマットはプリセット追加対応となる。MVP対応プリセット：りそな銀行（`resona`）・みずほ銀行（`mizuho`）・横浜銀行（`yokohama-bank`）。1組合が複数の BankCSVMapper を持てる。各マッパーには管理者が自由入力する `account_label`（例：「管理費口座」「修繕積立金口座」）を付与し、インポート時の選択肢として表示する。初回マッパーはオンボーディングで設定し、追加マッパーは設定ページで管理する。

### BankTransaction（銀行取引明細）
BankImport から生成される個々の取引行。PaymentProfile との照合によりどの Unit の入金かを特定し、PaymentRecord を生成する。

### PaymentRecord（入金記録）
特定の Unit の特定月における入金確認結果。PaymentStatus と実入金額・期待金額を保持する。

### PaymentStatus（月次支払い状態）
特定 Unit の特定月における入金確認状態。`confirmed`（確認済）・`missing`（未入金）・`irregular`（金額不一致）・`excluded`（対象外）のいずれか。`irregular` の場合は ! フラグを表示し、AnnualAccumulation に委ねる。

### AnnualAccumulation（年間累計照合）
遅延入金・一括入金などのイレギュラーに対応するため、月次ではなく年度累計の入金額が期待累計額と一致するかで最終的な入金完了を判断する仕組み。月次で `irregular` でも年間累計が一致すれば Unit のステータスは Green になる。

---

## 会計・報告

### FiscalYear（会計年度）
管理組合の会計上の1年間。開始月は組合ごとに設定可能（日本では4月始まりが多い）。

### Expense（支出）
管理組合が支出した費用。ExpenseCategory で科目分類される。

### ExpenseCategory（勘定科目）
支出を分類する科目。組織ごとにデフォルトテンプレートを提供し、カスタマイズ可能。管理費会計・修繕積立金会計を区別する。

### GeneralMeetingDocument（総会資料）
定期総会（年1回）で提示・承認される書類群。SaaS内のデータから自動生成しPDF出力する。出納の自主化には総会資料の自主作成が不可欠であり、管理会社依存を断ち切るために必須機能として位置づける。

### FinancialReport（収支報告書）
会計年度の収入・支出を科目別に集計した報告書。管理費会計と修繕積立金会計を分けて作成。総会資料の中核。月次版（理事会報告用）と年次版（総会用）がある。

### BalanceSheet（貸借対照表）
期末時点の資産（銀行残高・未収金など）と負債の状態を示す書類。総会資料に含まれる。

### BudgetProposal（次年度予算案）
総会で承認を求める次年度の収支予算。科目別の予算額を設定し、FinancialReport の予算対比欄に使用される。

### AuditReport（監査報告書）
監事による会計監査結果の報告書。総会資料に含まれる。監事ロールまたは管理者が作成・アップロードする。

---

## コラボレーション

### Topic（議題）
理事会・総会・日常業務における議題・問題・お知らせを表す単位。visibility により `board_only`（理事のみ）または `all_members`（全住民）を制御。

### Comment（コメント）
Topic に紐づく議論・意見の記録。

### Task（タスク）
Topic から生まれるアクションアイテム。担当者・期日・完了状態を持つ。

### Announcement（お知らせ）
住民または理事会向けの告知。visibility で公開範囲を制御。

### OperationsCalendar（業務カレンダー）
管理組合が実施すべき定期業務・イベントを管理するカレンダー。消防設備検査・エレベーター定期検査・貯水槽清掃・大規模修繕計画など、法定点検と任意業務を管理する。

### AnnualChecklist（年間業務チェックリスト）
年度内に必ず実施すべき業務の一覧。各項目に担当者・期日・完了確認を紐づける。日本のマンション管理に共通する標準テンプレート（ComplianceTask）を提供し、組合ごとにカスタム追加も可能。法定点検の抜け漏れ防止と引き継ぎ資産として機能する。MVP必須。

### ComplianceTask（法定点検タスク）
法律・条例で実施が義務付けられている定期点検。消防設備検査（年1回）・建築設備検査（年1回）・エレベーター定期検査（年1回）・貯水槽清掃（年1回）・特定建築物定期調査（3年ごと）などが該当。AnnualChecklist のテンプレートとして事前定義する。

### DeviceStrategy（デバイス方針）
管理者画面（理事・会計・理事長）はデスクトップ前提で設計する。住民向け画面（ResidentProfile・お知らせ確認）はモバイルファーストで設計する。Next.js＋Tailwindでレスポンシブ対応するが、複雑な管理操作（CSVインポート・PDF出力・収支入力）はデスクトップ使用を前提とする。

### Notification（通知）
システムが自動生成するアラート。管理者向け（滞納検知・タスク期限・CSV未照合件数など）と住民向け（お知らせ・支払い未確認など）がある。MVPの配信手段はメール＋アプリ内通知。LINE通知は Phase 3。住民のログイン頻度が低いため、メールが主要な到達手段となる。
